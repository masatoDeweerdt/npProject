<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Node.js News</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:wght@300;400;600&display=swap"
        rel="stylesheet">
    <script src="https://kit.fontawesome.com/b265f8cbaa.js" crossorigin="anonymous"></script>
</head>

<body>
    <header class="header">
        <div class='navMenu' id='navMenuId'><a href='' class='main_links'>Home</a>
            <div class='dropdown'><button class='dropbtn'>User<i class='fa fa-caret-down'>&#9660;</i></button>
                <div class='dropdown-content'><a href='/users/logout'>logout</a></div>
            </div><a class='icon' href='javascript:void(0);' onclick='openMenu()'>&#9776;</a>
        </div>
        <div class="header__logo">Tech News</div>

        <a href="/users/logout" type="submit" class="logout">Logout</a>

        <%- include('./partials/search') %>
    </header>

    <div class="wrapper">
        <div class="news">
            <% if(articles != null) { %>
            <% articles.forEach(function(article, index){ %>
            <a href="/article/<%- article.id %>" class="news__card">
                <img src="<%- article.jetpack_featured_media_url %>" alt="<%- article.title.rendered %>">
                <h2><%- article.title.rendered %></h2>
                <p><%- article.excerpt.rendered %></p>
            </a>
            <% }) %>
            <% } else { %>
            No posts found.
            <% } %>
        </div>
    </div>




</body>

</html>